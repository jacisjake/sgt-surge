# ─── Schwab Authentication ───────────────────────────────────────────────────
# NEVER commit your actual .env file to version control.
#
# 1. Create a developer app at https://developer.schwab.com
# 2. Note your App Key and App Secret
# 3. Set SCHWAB_ACCOUNT_HASH to your account's hashed account number
#    (retrieved via GET /accounts after completing OAuth flow)
#
SCHWAB_APP_KEY=your_app_key
SCHWAB_APP_SECRET=your_app_secret
SCHWAB_ACCOUNT_HASH=your_account_hash

# OAuth redirect URI — must match what you registered in the Schwab developer portal.
# Default works if running behind the standard Caddy reverse proxy.
# SCHWAB_OAUTH_REDIRECT_URI=https://your-domain.com/schwab/oauth/callback

# Path to persist the OAuth token across restarts (relative to project root).
# SCHWAB_TOKEN_PATH=state/schwab_token.json

# ─── Trading Mode ──────────────────────────────────────────────────────────────
# 'dry_run' = simulated fills (no real money), 'live' = real money.
# There is no 'paper' value for TRADING_MODE (use dry_run, or the separate
# run_paper_forward.sh paper ledger for strategy forward-tests).
TRADING_MODE=dry_run

# ORB real-money gate (default false). Even with TRADING_MODE=live, the ORB bot
# will not submit broker orders unless ENABLE_ORB_LIVE=true. Keep false for
# Trading Lab cutover / capital safety.
ENABLE_ORB_LIVE=false

# ─── Risk Management ──────────────────────────────────────────────────────────
# Max risk per trade as fraction of account (0.02 = 2%)
MAX_POSITION_RISK_PCT=0.02

# Max total portfolio risk as fraction of account (0.10 = 10%)
MAX_PORTFOLIO_RISK_PCT=0.10

# Max concurrent open positions
MAX_POSITIONS=5

# Max drawdown before halting trading (0.15 = 15%)
MAX_DRAWDOWN_PCT=0.15

# ─── Logging ──────────────────────────────────────────────────────────────────
# DEBUG, INFO, WARNING, or ERROR
LOG_LEVEL=INFO

# ── Alerting (email over SMTP) ───────────────────────────────────────────
# Schwab refresh tokens expire 7 days after creation and CANNOT be renewed via
# any API call — only a manual OAuth re-consent mints a new one. These alerts
# warn you BEFORE it dies (and shout when it does), so the bot never sits down
# silently for days. Leave unset to disable alerting (it just logs instead).
#
# Gmail: create an App Password (requires 2FA) and use it as SMTP_PASSWORD.
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
ALERT_EMAIL_TO=
ALERT_WARN_WITHIN_DAYS=2
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
